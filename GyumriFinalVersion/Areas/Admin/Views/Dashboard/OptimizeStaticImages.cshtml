@if (TempData["Message"] != null)
{
    <div style="color: green">@TempData["Message"]</div>
}

<form asp-action="OptimizeStaticImages" method="post">
    <button type="submit">Run Image Optimizer</button>
</form>
@if (TempData["Message"] != null)
{
    <div class="alert alert-info">
        @TempData["Message"]
    </div>
}
